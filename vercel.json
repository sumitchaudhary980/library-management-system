{
  "version": 2,
  "regions": ["bom1"],
  "builds": [
    {
      "src": "backend/src/server.js",
      "use": "@vercel/node",
      "config": { "includeFiles": "frontend/**" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "backend/src/server.js" }
  ]
}